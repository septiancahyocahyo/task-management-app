/**
 * DashboardPage — Halaman utama setelah login
 * Route: /dashboard (protected — harus login)
 *
 * Placeholder: akan menampilkan daftar task, filter, dan tombol tambah task.
 * Integrasi API dan styling premium akan ditambahkan di tahap berikutnya.
 */
export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Halaman dashboard — akan menampilkan daftar task setelah integrasi API.</p>
    </div>
  )
}
